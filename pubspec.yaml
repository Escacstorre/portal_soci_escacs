name: portal_soci_escacs
description: "Portal de Socis — Club d'Escacs Torredembarra"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0-235.0.dev

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_svg: ^2.0.17
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
